# This file configures the endpoints Rasa uses.

# Custom action server
action_endpoint:
  url: "http://localhost:5055/webhook"

# Use default in-memory tracker store
# (Commented out Redis and Mongo unless you're actually using them)
# tracker_store:
#   type: redis
#   url: localhost
#   port: 6379
#   db: 0
#   password: null
#   use_ssl: false

# tracker_store:
#   type: mongod
#   url: mongodb://localhost:27017
#   db: rasa
#   username: your-username
#   password: your-password

# Event broker is optional — not needed here
# event_broker:
#   type: pika
#   url: localhost
#   username: username
#   password: password
#   queue: rasa_events
